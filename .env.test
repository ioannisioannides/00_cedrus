# Test environment - intentionally empty.
# Forces Django to use the default SQLite database for tests.
# The main .env sets DATABASE_URL for Docker/Postgres which is not
# available when running tests locally.
