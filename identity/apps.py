from django.apps import AppConfig


class IdentityConfig(AppConfig):
    """Django app configuration for the identity application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "identity"
