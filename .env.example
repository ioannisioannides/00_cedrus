# Production Environment Variables Template
# Copy this file to .env and fill in your production values
# NEVER commit .env to git (it's in .gitignore)

# =============================================================================
# CRITICAL SECURITY SETTINGS
# =============================================================================

# Generate a new secret key for production:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your-secret-key-here-minimum-50-characters

# Comma-separated list of allowed hostnames
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

DB_NAME=cedrus_production
DB_USER=cedrus
DB_PASSWORD=your-database-password-here
DB_HOST=localhost
DB_PORT=5432

# =============================================================================
# EMAIL CONFIGURATION (Optional)
# =============================================================================

# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-email-app-password
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================

# Redis Cache (if using)
# REDIS_URL=redis://127.0.0.1:6379/1

# Sentry Error Tracking (if using)
# SENTRY_DSN=https://your-sentry-dsn

# Django Log Level (default: INFO)
# DJANGO_LOG_LEVEL=INFO

# =============================================================================
# DEPLOYMENT INSTRUCTIONS
# =============================================================================

# 1. Copy this file:
#    cp .env.example .env

# 2. Fill in all required values above

# 3. Load environment variables:
#    export $(cat .env | xargs)

# 4. Or use django-environ package:
#    pip install django-environ
#    # Then update settings_production.py to use environ.Env()

# 5. Run deployment checks:
#    export DJANGO_SETTINGS_MODULE=cedrus.settings_production
#    python manage.py check --deploy

# 6. Apply migrations:
#    python manage.py migrate

# 7. Collect static files:
#    python manage.py collectstatic --noinput

# 8. Create superuser:
#    python manage.py createsuperuser

# 9. Start application:
#    gunicorn cedrus.wsgi:application --bind 0.0.0.0:8000
