from django.apps import AppConfig


class AuditsConfig(AppConfig):
    """Django app configuration for the audits application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "audits"
